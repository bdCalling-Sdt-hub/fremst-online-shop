import { Model, Types } from 'mongoose'

export type IAdmin = {
  // Define the interface for Admin here
};



export type AdminModel = Model<IAdmin>;


export type CompanyProductPrice = {

  product:Types.ObjectId;
  price:number;
}